@{
    ViewBag.Title = "Control Panel";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="container-xxl flex-grow-1 container-p-y">
    <div class="row justify-content-center align-items-center h-100">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header">
                    <h3>Floral Haven Admin</h3>
                    <h4 class="card-title">
                        Control Panel
                    </h4>
                </div>
                <div class="card-body">
                    <div class="list-group">
                        <a href="@Url.Action("UserList", "User")" class="list-group-item list-group-item-action">User List</a>
                        <a href="@Url.Action("Create", "User")" class="list-group-item list-group-item-action">Create User</a>
                        <a href="@Url.Action("ProductList", "Product")" class="list-group-item list-group-item-action">Product List</a>
                        <a href="@Url.Action("Create", "Product")" class="list-group-item list-group-item-action">Create Product</a>
                        <a href="@Url.Action("OrderList", "User")" class="list-group-item list-group-item-action">Order List</a>

                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
